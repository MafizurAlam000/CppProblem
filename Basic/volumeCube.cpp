//WAP to calculates the volume of cube
#include<iostream>
using namespace std;
int main(){
    float a;
    cout<<"Enter the length of one side of the cube: ";
    cin>>a;
    float volume;
    volume=(a*a*a);
    cout<<"Volume of the cube is: "<<volume<<endl;
    return 0;
}